namespace MyClasses
{
    public enum SqlType
    {
        INTEGER,
        REAL,
        NUMERIC,
        BIGINT,
        TEXT,
        BLOB,
        DATE,
        DATETIME,
        TIMESTAMP,
    }
}
